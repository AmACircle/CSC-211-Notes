name: Build C++ Executables

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write  # Allows pushing changes to the repository

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Create build directory
        shell: pwsh
        run: New-Item -ItemType Directory -Path build -Force

      - name: Compile C++ files (Windows)
        shell: pwsh
        run: |
          Get-ChildItem -Filter *.cpp | ForEach-Object {
            $exeName = "build\" + $_.BaseName + ".exe"
            cl /EHsc /Fe:$exeName $_.FullName
          }

      - name: Commit and push build results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add build
          git commit -m "Add Windows build artifacts [CI]"
          git push
        continue-on-error: true  # Prevent failure if no changes

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create build directory
        run: mkdir -p build

      - name: Compile C++ files (macOS) with Clang
        run: |
          for file in *.cpp; do
            clang++ -std=c++17 -o build/"${file%.cpp}" "$file"
          done

      - name: Commit and push build results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add build
          git commit -m "Add macOS build artifacts [CI]"
          git push
        continue-on-error: true  # Prevent failure if no changes
